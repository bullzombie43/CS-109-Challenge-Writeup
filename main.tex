\documentclass{article}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{listings}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage[numbers]{natbib}
\graphicspath{ {./images/} }


\title{Real or Fake? Detecting Deepfakes}
\author{Justin Hollister}
\date{March 2026}

\begin{document}

\maketitle

If you have spent time on social media in the last few months you have almost certainly encountered a variety of AI generated images and videos.  Maybe you've seen a video of your English teacher dunking on Lebron \footnote{https://www.tiktok.com/@ikrrrrbwi81alt/video/7598717828409068855}, or perhaps an altered image posted by our White House\footnote{https://www.theguardian.com/us-news/2026/jan/22/white-house-ice-protest-arrest-altered-image}. With apps like Viggle Ai\footnote{It took <5 min to make a deepfake. I used an animated character but this could just as easily have been a person https://viggle.ai/share/436f2b0d-14e5-4705-9ea4-2078e33d2c76 } it is easier than ever to create and promote deepfaked images and videos of people. While these videos, when clearly false, can be very amusing, they are also capable of great societal harm. In this paper I explore and compare a few methods of detecting deepfakes via techniques covered in CS109: Naive Bayes and Logistic Regression.

\subsection*{Methods and Data}
There are dozens of different approaches to create deepfakes, some of which include: Generative Adversarial Networks and Encoder-Decoder Networks for faceswaps, mesh/landmark based and end to end models for face reenactment, diffusion models for full face generation and much more. Every method leaves its own form of traces, so it is difficult to generalize a single model to detect all types of deepfakes. For my project I focused on deepfakes of faces specifically, training for and testing on the FaceForensics++~\cite{rossler2019faceforensics} benchmark dataset.

\subsection*{Qualities of a Deepfake}
Deepfaked Videos and images, while difficult to spot with the human eye, often leave traces that computers can pick up on. Often, deepfaked videos, especially those made by Viggle, will overlay two videos on top of each other. If we compare the face region of a video with the background the compression artifacts and color grading may be different. Between frames the color grading of a face is also likely to vary more in a deepfake then with a normal video. For every video I extracted 5 features which should, in theory, differ between deepfaked and normal videos: DCT\footnote{Discrete Cosine Transform --- a technique that expresses an image patch as a weighted sum of cosine waves at increasing spatial frequencies. It is the mathematical core of JPEG and most video compression standards (H.264, H.265).} divergence between face and background, block-DCT divergence for compression differences, mean difference between DCT coefficients, frame to frame MAD\footnote{Mean Absolute Deviation --- the average of the absolute pixel-wise differences between two image patches, measuring how much they differ per pixel on average.} of face crops, and laplacian variance ratio of face and background. This paper is focusing on the techniques used to analyze these features, as such the specifics of the features themselves is not important, it is sufficient to assume that differences in each of these values exists between deepfakes and normal videos. That said, if you are interested in the specifics of each feature and how I came to use them, I include detailed descriptions in the appendix.

\subsection*{Naive Bayes}
Naive Bayes is a machine learning algorithm for classification which makes the assumption that all features are independent of each other. With any classification\footnote{for all our equations we are assuming that y is binary ie. $y={0,1}$ as a video can only be a deepfake or not a deepfake} algorithm, we seek to choose the value $y$ that maximizes $P(Y = y | X = X)$. 
\begin{equation}
\begin{aligned}
\hat y &= \text{arg max} \ P(Y = y | X = x) \\
&= \text{arg max} \ \frac{P(Y = y)P(X = x | Y = y)}{P(X = x)} \\
&= \text{arg max} \ P(Y = y)P(X = x | Y = y) \text{(since $P(X=x)$ is a constant w.r.t. $Y$}
\end{aligned}
\end{equation}
Naive Bayes makes the incorrect assumption that each characteristic $x$ is independent of one another given y, reducing the space needed from $O(n^2)$ to $O(n)$ if $|x| = n$.
\begin{equation}
\begin{aligned}
\hat y &= \text{arg max} \ P(Y = y)P(X = x | Y = y) \\
&= \text{arg max} \ P(Y = y)\prod_i P(X_i = x_i | Y = y) \\
&= \text{arg max} \ \log P(Y = y) + \sum_i \log P(X_i = x_i | Y = y)
\end{aligned}
\end{equation}

\subsubsection*{Maximum Likelihood Estimation}

With equation 2 we can estimate the probability of a deepfake if we have the probabilities of each feature for both deepfakes and normal videos, but where do those come from? This is where the "training" part of our model is introduced. Essentially for both deepfakes and normal videos we will create a probability distribution of each feature $x_i$. To calculate each $P(X_i = x_i | Y =y)$ we will use the probability mass function of these distributions. To find these distributions, we will employ a technique, Maximum Likelihood Estimation\footnote{MLE makes the assumption that our data is identically distributed}, where we will choose values for our parameters $\theta$ that maximize the likelihood of our data. 

\begin{equation}
\begin{aligned}
L(\theta) = \prod_{i=1}^n f(X_i = x_i | \Theta = \theta) \\
\hat \theta = \text{arg max} \ L(\theta) 
\end{aligned}
\end{equation}

Because log is a monotone function\footnote{A monotonically increasing function preserves order: if $a > b$ then $f(a) > f(b)$. Because $\log$ is monotone increasing, $\arg\max_\theta \log L(\theta) = \arg\max_\theta L(\theta)$, so maximising the log-likelihood gives the same parameter estimates as maximising the likelihood directly.}, the argmax of our log likelihood will be equal to the argmax of our likelihood simplifying the math.

\begin{equation}
\begin{aligned}
LL(\theta) &= \log L(\theta) \\
&= \log \prod_{i=1}^n f(X_i = x_i | \Theta = \theta) \\
&= \sum_{i = 1}^n \log f(X_i = x_i | \Theta = \theta)
\end{aligned}
\end{equation}

To use MLE we must guess at the underlying distribution of our features. MLE is compatible any distribution provided you can define it's PDF and PMF. I started by assuming most of my features were normal, however after examining the graphed data I also tried a Gamma\footnote{The Gamma distribution is a two-parameter continuous distribution supported on $(0, \infty)$, parameterised by shape $\alpha > 0$ and scale $\beta > 0$. It generalises the exponential distribution and can model right-skewed, non-negative data. See \url{https://en.wikipedia.org/wiki/Gamma_distribution}.} distribution. 

\begin{figure}
    \centering
    \includegraphics[width=16cm]{images/f5_distribution_all_fits.png}
    \caption{A visualization of our f5 feature and how a Gaussian vs Gamma vs KDE assumption fits it for both real and deepfaked videos.}
    \label{fig:gamma_vs_guassian_visualized}
\end{figure}

\subsubsection*{Normal MLE Estimation}
A Gaussian has two parameters we need to estimate: $\mu = \theta_0, \sigma^2 = \theta_1 $ 
\begin{equation}
\begin{aligned}
L(\theta) &= \prod_{i=1}^n f(X_i|\theta) \\
&= \prod_{i=1}^n \frac{1}{\sqrt{2\pi\theta_1}}e^{-\frac{(x_i-\theta_0)^2}{2\theta_1}} \\
LL(\theta) &= \sum_{i=1}^n \left[-\log\sqrt{2\pi\theta_1} - \frac{1}{2\theta_1}(x_i - \theta_0)^2\right] \\
&= -\frac{n}{2}\log(2\pi\theta_1) - \frac{1}{2\theta_1}\sum_{i=1}^n(x_i - \theta_0)^2 \\
\frac{\partial LL(\theta)}{\partial \theta_0} &= \frac{1}{\theta_1}\sum_{i=1}^n(x_i - \theta_0) = 0
\implies \hat{\theta}_0 = \frac{1}{n}\sum_{i=1}^n x_i = \bar{x} \\
\frac{\partial LL(\theta)}{\partial \theta_1} &= -\frac{n}{2\theta_1} + \frac{1}{2\theta_1^2}\sum_{i=1}^n(x_i - \theta_0)^2 = 0
\implies \hat{\theta}_1 = \frac{1}{n}\sum_{i=1}^n(x_i - \bar{x})^2
\end{aligned}
\end{equation}

\subsubsection*{Gamma MLE Estimation}
A Gamma has two parameters we need to estimate: $\alpha = \theta_0, \beta  = \theta_1 $ 
\begin{equation}
\begin{aligned}
L(\theta) &= \prod_{i=1}^n f(X_i|\theta) \\
&= \prod_{i=1}^n \frac{1}{\Gamma(\theta_0)\theta_1^{\theta_0}} x_i^{\theta_0-1} e^{-x_i/\theta_1} \\
LL(\theta) &= \sum_{i=1}^n \left[-\log\Gamma(\theta_0) - \theta_0\log\theta_1 + (\theta_0-1)\log x_i - \frac{x_i}{\theta_1}\right] \\
&= -n\log\Gamma(\theta_0) - n\theta_0\log\theta_1 + (\theta_0-1)\sum_{i=1}^n\log x_i - \frac{1}{\theta_1}\sum_{i=1}^n x_i
\end{aligned}
\end{equation}
There is unfortunately no closed form solution for the Gamma MLE estimation, it must instead be estimated numerically. I used \verb|scipy.stats.gamma.fit|\footnote{\url{https://docs.scipy.org/doc/scipy/reference/generated/scipy.stats.gamma.html} --- \texttt{scipy.stats.gamma.fit} uses maximum likelihood estimation via the method of moments as a starting point and then refines numerically using \texttt{scipy.optimize}.}.

\subsubsection*{Kernel Density Estimation}
If you looked closely at figure 1 earlier, you may have noticed the existence of a third line of fit ``KDE (Scott)'',
which actually matches our data better than both versions of MLE. Kernel Density Estimation
\footnote{https://faculty.washington.edu/yenchic/17Sp\_403/Lec7-density.pdf}, or KDE, is a non-parametric density
estimation technique, meaning unlike MLE it makes no assumption about the underlying distribution of our data. Instead,
KDE constructs a smooth estimate of the PDF by placing a \textit{kernel} --- a small symmetric bump function --- centered
at each data point and summing them together.\footnote{KDE does not change how Naive Bayes works nor its underlying assumption about independence, it only changes how we estimate the probability of each given feature}

\begin{equation}
\hat{f}_h(x) = \frac{1}{nh} \sum_{i=1}^n K\!\left(\frac{x - x_i}{h}\right)
\end{equation}

where $K$ is the kernel function and $h > 0$ is the \textit{bandwidth}, which controls the smoothness of the estimate.
The most common choice is the Gaussian kernel:

\begin{equation}
K(u) = \frac{1}{\sqrt{2\pi}} e^{-\frac{u^2}{2}}
\end{equation}

The bandwidth $h$ is the key tuning parameter of KDE\footnote{Too small a bandwidth leads to an overfit, noisy estimate;
too large and the estimate is oversmoothed and loses structure}. Scott's rule, which is what we use here, estimates an
optimal bandwidth automatically from the data:

\begin{equation}
h = n^{-1/5} \hat{\sigma}
\end{equation}

where $\hat{\sigma}$ is the standard deviation of the sample. Once we have our KDE estimate $\hat{f}_h(x)$, we use it
directly in place of the parametric PDF from MLE to compute $P(X_i = x_i \mid Y = y)$ for each feature.

While it was outside the scope of CS109, I was curious how KDE would perform compared to the MLE approach for Naive Bayes, as our data does not seem to follow any clean parametric family. I used \verb|sklearn.neighbors.KernelDensity| with \verb|kernel='gaussian'| and \verb|bandwidth='scott'|.

\subsection*{Logistic Regression}
Logistic regression is also a classification algorithm. It makes the central assumption that $P(y|x)$ can be approximated with a sigmoid function applied to a linear combination of input features. 
\begin{equation}
\begin{aligned}
P(Y = 1 | X = x) = \sigma(z) \text{ where } z = \theta_0 + \sum_{i=1}^m \theta_i x_i
\end{aligned}
\end{equation}
Logistic similarly uses MLE to estimate parameters, however it requires the assumption that $Y \sim Bern(p)$ where $p = \sigma(\theta^Tx)$. 


\subsubsection*{Bernoulli MLE}

\begin{equation}
\begin{aligned}
L(\theta) &= \prod_{i=1}^n P(Y = y^{(i)} | X = \mathbf{x}^{(i)}) \\
&= \prod_{i=1}^n \sigma(\theta^T \mathbf{x}^{(i)})^{y^{(i)}} \cdot \left[1 - \sigma(\theta^T \mathbf{x}^{(i)})\right]^{(1-y^{(i)})} \\
LL(\theta) &= \sum_{i=1}^n y^{(i)} \log \sigma(\theta^T \mathbf{x}^{(i)}) + (1 - y^{(i)}) \log\left[1 - \sigma(\theta^T \mathbf{x}^{(i)})\right]
\end{aligned}
\end{equation}

We use the gradient descent algorithm\footnote{Gradient descent iteratively updates parameters in the direction of steepest descent of the loss: $\theta \leftarrow \theta - \alpha \nabla_\theta (-LL(\theta))$, where $\alpha$ is the learning rate. Since there is no closed-form solution for the logistic regression MLE, this numerical approach is used instead.} to estimate parameters($\theta$) that maximize our likelihood.

\subsubsection*{Youden's J Threshold}
If our model produces a probability $P(\text{fake} | X = x)$ that any video is a deepfake, how do we interpret that probability? A naive approach is to classify a video as fake if $P(\text{fake} | X = x) > 0.5$.\footnote{We used an even split of deepfakes and real videos in both training and test, so 0.5 makes sense here. If instead the split was 0.7 and 0.3 we might use a different default.} However our raw probability scores (what MLE or KDE outputs) could be systematically biased towards one side. A common solution to this is to calibrate your model, with a method such as Platt's scaling\footnote{Platt scaling fits a sigmoid function to the raw output scores of a classifier using a held-out calibration set, transforming them into calibrated probabilities that better reflect the true likelihood of each class.}. You want to calibrate your model such that its output probabilities reflect the actual distribution of reality. I was interested in finding a simpler solution, which only adjusted the threshold for classification and came across Youden's J Statistic. 

Once we have our posterior probability estimates from Bayes' theorem, we must choose a decision threshold $\tau$ such 
that we classify a video as a deepfake if $P(Y = 1 | \mathbf{x}) > \tau$. The naive choice of $\tau = 0.5$ is not 
always optimal, particularly in the presence of class imbalance. Instead, we use Youden's J Statistic 
\footnote{Youden, W.J. (1950). Index for rating diagnostic tests. \textit{Cancer}, 3(1), 32--35.} to select the 
threshold that best balances sensitivity and specificity:

\begin{equation}
J = \text{Sensitivity} + \text{Specificity} - 1 = \text{TPR} - \text{FPR}
\end{equation}

where TPR (True Positive Rate) and FPR (False Positive Rate) are defined as:

\begin{equation}
\text{TPR} = \frac{TP}{TP + FN} \qquad \text{FPR} = \frac{FP}{FP + TN}
\end{equation}

The optimal threshold is then chosen as:

\begin{equation}
\hat{\tau} = \underset{\tau}{\text{arg max}} \ J(\tau) = \underset{\tau}{\text{arg max}} \ [\text{TPR}(\tau) - \text{FPR}(\tau)]
\end{equation}

Geometrically, this corresponds to finding the point on the ROC curve that is furthest from the diagonal line of 
no-discrimination, maximizing the gap between our model's performance and random chance.

\subsection*{Results}

\subsubsection*{f5 only:}

  \begin{table}[h]
  \centering
  \begin{tabular}{lccccc}
  \hline
  \textbf{Method} & \textbf{Accuracy} & \textbf{AUC} & \textbf{Precision} &
  \textbf{Recall} & \textbf{F1} \\
  \hline
  KDE-NB          & \textbf{0.688} & \textbf{0.683} & \textbf{0.742} & 0.575 &
  0.648 \\
  Logistic Reg.   & 0.588 & 0.680 & 0.559 & \textbf{0.825} & \textbf{0.667} \\
  Gaussian NB     & 0.588 & 0.680 & 0.559 & \textbf{0.825} & \textbf{0.667} \\
  Gamma NB        & 0.588 & 0.671 & 0.559 & \textbf{0.825} & \textbf{0.667} \\
  \hline
  Majority class  & 0.500 & 0.500 & 0.500 & 1.000 & 0.667 \\
  Random          & $\approx$0.500 & $\approx$0.500 & $\approx$0.500 & $\approx$0.500 & $\approx$0.500 \\
  \hline
  \end{tabular}
  \caption{Results comparison — f5 only (n=80 test videos). Baselines: \textit{Majority class} always predicts ``fake''; \textit{Random} flips a fair coin per video.}
  \label{tab:results_f5}
  \end{table}

\subsubsection*{All 5 features:}

  \begin{table}[h]
  \centering
  \begin{tabular}{lccccc}
  \hline
  \textbf{Method} & \textbf{Accuracy} & \textbf{AUC} & \textbf{Precision} &
  \textbf{Recall} & \textbf{F1} \\
  \hline
  Logistic Reg.   & \textbf{0.688} & \textbf{0.720} & \textbf{0.674} &
  \textbf{0.725} & \textbf{0.699} \\
  KDE-NB          & 0.637 & 0.670 & 0.628 & 0.675 & 0.651 \\
  Gaussian NB     & 0.600 & 0.666 & 0.580 & 0.725 & 0.644 \\
  Gamma NB        & 0.588 & 0.655 & 0.569 & 0.725 & 0.637 \\
  \hline
  Majority class  & 0.500 & 0.500 & 0.500 & 1.000 & 0.667 \\
  Random          & $\approx$0.500 & $\approx$0.500 & $\approx$0.500 & $\approx$0.500 & $\approx$0.500 \\
  \hline
  \end{tabular}
  \caption{Results comparison — all 5 features (n=80 test videos). Baselines as defined in Table~\ref{tab:results_f5}.}
  \label{tab:results_all5}
  \end{table}

\textbf{Accuracy}: fraction of videos correctly classified. 
\textbf{AUC}: area under the ROC\footnote{Receiver Operating Characteristic curve --- a plot of True Positive Rate (TPR) against False Positive Rate (FPR) at every possible classification threshold. A perfect classifier reaches the top-left corner (TPR=1, FPR=0); the diagonal represents random chance. AUC summarises the entire curve as a single number between 0 and 1.} curve; measures ranking ability independent of threshold.
\textbf{Precision}: of videos predicted fake, the fraction that are truly fake.
\textbf{Recall}: of all truly fake videos, the fraction correctly identified. 
\textbf{F1}: harmonic mean of precision and recall.


I divided the results into two categories, one only using f5\footnote{f5 is the Laplacian variance ratio --- the variance of the Laplacian filter response in the face region divided by that of the background. See the Feature Descriptions appendix for the full derivation.}, and one using all 5 features. f5 was shown to be our most discriminating feature by far\footnote{As measured by AUC (Area Under the ROC Curve): f5 achieved a standalone AUC of 0.683, the highest of all five features.}, so I thought it would be interesting to examine how it would perform independently of the other features. 

In both categories the KDE version of our Naive Bayes model performed better than both the Gaussian and Gamma model, validating our assumption that the data did not fit either parametric model particularly well. Importantly, all 3 methods have nearly identical AUC, suggesting the improvement does not come from the ability to distinguish between classes\footnote{AUC measures ranking ability --- the probability that the model scores a fake video higher than a real one --- independently of any threshold. Two models with the same AUC order videos identically; what differs is the absolute probability values they assign. KDE's better-calibrated densities push fakes closer to 1 and reals closer to 0, so the Youden's J threshold lands in a cleaner spot.}, but rather from the classification cutoff. 

Interestingly, depending on our feature set (f5 or all) we have two different top methods, both with an accuracy of 0.688: KDE-NB for just f5 and Logistic Regression for all 5 features. The loss in accuracy, 0.051, of KDE-NB when we introduce all the features suggests that the independence assumption of Naive Bayes may be heavily violated --- our features are highly related, to such an extent that including other features reduces our accuracy noticeably. Looking at Figure~\ref{fig:confusion_matrices_all_methods} we can see that we have a nearly doubled false positive rate when including all features for KDE-NB, which makes sense as the independence assumption means we are essentially double counting the likelihood of each feature. 

Logistic regression does not have this issue. It doesn't assume each feature independent but rather that the probability can be approximated by sigmoid instead, which is why it performed significantly better with all features compared to just one. With a single feature, logistic regression is very similar to Gaussian NB, even equivalent if the variances of both classes are identical, which is likely why it performs nearly identically to Gaussian NB with only f5. 

Overall, while KDE-NB and Logistic Regression have the same accuracy, I give the edge to logistic regression based on its higher ($0.037$) AUC, indicating it has a stronger ability to distinguish between classes, and that it might perform better than KDE-NB on a larger testing set.  


\begin{figure}
    \centering
    \includegraphics[width=0.75\linewidth]{images/confusion_matrices_all_methods.png}
    \caption{Confusion matrices for all methods (rows: f5-only and all-5-features configurations). Each cell shows the count out of 80 test videos. Note the higher false-positive rate when KDE-NB uses all five features, consistent with the NB independence assumption being violated.}
    \label{fig:confusion_matrices_all_methods}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.85\linewidth]{images/roc_comparison_all_methods.png}
    \caption{ROC curves for all methods and feature configurations. The diagonal dashed line represents random chance (AUC = 0.500). Logistic Regression with all five features achieves the highest AUC (0.720), while all methods substantially outperform the random baseline regardless of feature set.}
    \label{fig:roc_all_methods}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.75\linewidth]{images/score_distribution_f5.png}
    \caption{Posterior score distributions ($P(\text{fake} \mid \mathbf{x})$) for the KDE-NB classifier using f5 only. Real videos (blue) are concentrated near 0 and deepfakes (orange) near 1, with meaningful separation despite overlap. The vertical dashed line marks the Youden's J threshold ($\tau \approx 0.513$).}
    \label{fig:score_distribution_f5}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.75\linewidth]{images/segmentation_test.png}
    \caption{Example face segmentation output from the OpenCV Haar Cascade detector. The tighter bounding box (15\% inset left/right, 10\% top, 20\% bottom) focuses on the core face region, reducing contamination from hair and neck.}
    \label{fig:segmentation}
\end{figure}

\clearpage
\subsection*{The Future of Deepfake Detection}
The best result we were able to achieve was an accuracy of 68.8 percent, meaning we still misidentify over 30 percent of videos. Our model did not treat a false positive or false negative differently, though one might argue that in the field of deepfake detection a false negative, falsely classifying a deepfaked video as real, to be much more harmful. Modern deepfake detection systems generally use deep learning to achieve accuracies above 99 percent on benchmarks like FF++ --- substantially better than our 68.8\%. Most of these deep learning models operate directly on pixels (or a combination) rather than through predefined features like ours, which requires significantly more training data. I trained my model on 320 videos (200 deepfakes and 200 real total, 80/20 train/test split) and tested on 80 videos of an even split. For reference FF++ has 5000 videos total, and other datasets have far more. My model is far from effective, but it was a genuinely interesting exercise in probabilistic classifiers.

\subsection*{LLM Usage}
I used LLMs, specifically Claude Code's Sonnet 4.6, to help me program much of first version of the code. All of the feature extraction code is done by LLM, as like I mentioned in the writeup, my focus was on the classification methods, and much of the detail of each features are outside my domain of knowledge (the require in depth knowledge about image processing and compression and more). I tested dozens of variations of features, and used LLMs to quickly implement features to test and verify the accuracy before I devoted too much time into anything. I also used LLMs to generate the initial code for classification, again to verify the approach and accuracy, before going back through and rewriting functions that used libraries such as \verb|scipy| and \verb|sklearn|, with my own implementations. I also used LLM's to refine this paper (ie. check for typos, errors, shorten my argument to reach 3 pages etc.). 

\newpage
\appendix
\section{Feature Descriptions}

For every video in the dataset I extract 5 scalar features. Each feature compares a \textit{face region} to a \textit{background region} within the same frame. The face is detected with OpenCV's Haar Cascade classifier, and the bounding box is tightened by 15\% on the left and right, 10\% on the top, and 20\% on the bottom to focus on the core face and reduce hair/neck contamination. The background is the remainder of the frame after removing the (un-tightened) bounding box. Ten frames are sampled uniformly from each video, and per-frame feature values are averaged to produce a single video-level feature vector.

\subsection*{f1 — DCT Chi-Squared Divergence}

\textbf{Intuition.} FaceSwap pastes a synthetically generated face onto an unmodified background. The face region has been processed through at least one additional encode-decode cycle, so its frequency-domain content should look systematically different from the native background.

\textbf{Computation.} Both the face crop and the background region are converted to grayscale and a 2D Discrete Cosine Transform (DCT) is applied to each. The DCT expresses an image patch as a sum of cosine waves at different spatial frequencies; compression artifacts show up as anomalies in the coefficient magnitudes. We then build a 50-bin normalized histogram of the DCT coefficients for each region:

\[
  P_k = \frac{\text{count}(\text{face coefficients in bin } k)}{\text{total face coefficients}}, \quad
  Q_k = \frac{\text{count}(\text{background coefficients in bin } k)}{\text{total background coefficients}}
\]

The chi-squared divergence between these two distributions is the feature:

\[
  f_1 = \chi^2(P, Q) = \sum_{k=1}^{50} \frac{(P_k - Q_k)^2}{P_k + Q_k + \varepsilon}
\]

A log transform ($\log(1 + f_1)$) is applied at training time to correct the heavy right skew of the raw values.

\subsection*{f2 — Block-DCT $\beta$-Statistic Divergence}

\textbf{Intuition.} JPEG and video codecs compress images in $8\times 8$ pixel blocks using the DCT. When a face region is re-encoded (as happens in FaceSwap), each AC coefficient position accumulates a different amount of quantization noise than the surrounding background. This changes the \textit{scale} of the coefficient distribution at each AC position in a measurable way, even on lossless (c0) source files where the original compression is preserved.

\textbf{Computation.} The face and background regions are each divided into non-overlapping $8\times8$ pixel blocks. A 2D DCT is applied to every block. For each of the 63 AC coefficient positions (the DC component at position 0 is skipped because it simply encodes average brightness), we collect the coefficient values across all blocks and compute the scale parameter:

\[
  \beta_j = \frac{\text{std}(\{c_{b,j} : b = 1,\ldots,B\})}{\sqrt{2}}, \quad j = 1, \ldots, 63
\]

This gives two 63-dimensional vectors, $\boldsymbol{\beta}^{\text{face}}$ and $\boldsymbol{\beta}^{\text{bg}}$. The feature is the mean absolute difference across all positions:

\[
  f_2 = \frac{1}{63} \sum_{j=1}^{63} \left| \beta_j^{\text{face}} - \beta_j^{\text{bg}} \right|
\]

This approach is based on Giudice et al.\ (2021)\footnote{Giudice et al., ``Fighting Deepfakes by Detecting GAN DCT Anomalies'', \textit{MDPI Journal of Imaging}, 2021.}. A log transform is applied at training time.

\subsection*{f3 — Absolute Difference of Mean Absolute DCT Coefficients}

\textbf{Intuition.} AI face synthesis often introduces a slight blurring or smoothing of the face relative to the original. In the frequency domain, blurring suppresses high-frequency coefficients, reducing the average coefficient magnitude. If the face is blurrier than the background we expect the mean absolute DCT coefficient to be lower for the face than for the background.

\textbf{Computation.} Full-image 2D DCTs are applied to the grayscale face and background patches. The mean of the absolute values of all coefficients is computed for each:

\[
  \mu_{\text{face}} = \frac{1}{MN}\sum_{u,v} |F_{\text{face}}[u,v]|, \quad
  \mu_{\text{bg}} = \frac{1}{M'N'}\sum_{u,v} |F_{\text{bg}}[u,v]|
\]

The feature is:

\[
  f_3 = |\mu_{\text{face}} - \mu_{\text{bg}}|
\]

A log transform is applied at training time.

\subsection*{f4 — Frame-to-Frame Temporal Inconsistency}

\textbf{Intuition.} FaceSwap estimates the warp for each frame independently, which introduces small jitter in the pasted face from one frame to the next that does not follow the smooth natural motion of a real face. We should therefore see higher frame-to-frame pixel variation in the face region of deepfakes than in real videos.

\textbf{Computation.} Consecutive pairs of sampled frames $(t, t+1)$ are used. Each face crop is resized to $64\times64$, converted to grayscale, and mean-normalized to remove global lighting drift:

\[
  \tilde{I}_t = \frac{I_t}{\text{mean}(I_t) + \varepsilon}
\]

The Mean Absolute Deviation (MAD) between consecutive normalized crops is computed, then averaged across all pairs:

\[
  f_4 = \frac{1}{T-1} \sum_{t=1}^{T-1} \frac{1}{64^2}\sum_{i,j}\left|\tilde{I}_t[i,j] - \tilde{I}_{t+1}[i,j]\right|
\]

where $T = 10$ is the number of sampled frames. No log transform is applied to $f_4$.

\subsection*{f5 — Laplacian Variance Ratio}

\textbf{Intuition.} FaceSwap applies a blending and sharpening step when compositing the generated face onto the background frame. This introduces ringing and edge-enhancement artifacts in the face region that raise its apparent sharpness above that of the surrounding (unmodified) background. The Laplacian filter is the standard tool for measuring image sharpness, since it amplifies rapid intensity changes (edges, texture, ringing).

\textbf{Computation.} The discrete Laplacian operator $\nabla^2$ is applied to the grayscale face and background:

\[
  \nabla^2 f[i,j] = f[i+1,j] + f[i-1,j] + f[i,j+1] + f[i,j-1] - 4f[i,j]
\]

The feature is the ratio of their variances:

\[
  f_5 = \frac{\text{Var}(\nabla^2 I_{\text{face}})}{\text{Var}(\nabla^2 I_{\text{bg}}) + \varepsilon}
\]

Higher $f_5$ means the face is sharper (or more artifact-laden) relative to the background. A log transform is applied at training time. $f_5$ was by far the strongest individual discriminator, achieving the best standalone AUC (0.683) of all five features.

\bibliographystyle{plainnat}
\bibliography{references}

\end{document}

